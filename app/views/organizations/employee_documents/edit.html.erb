<%= render PageComponent.new(title: "Editing employee document", content_container: true, full_width: false) do |component| %>
  <%= render "form", employee_document: @employee_document %>
  <%= link_to "Cancel", @employee_document, class: "btn btn-secondary" %>
<% end %>
