<%= render PageComponent.new(title: "Editing employee", content_container: true, full_width: false) do |component| %>
  <%= render "form", employee: @employee %>
  <%= link_to "Cancel", @employee, class: "btn btn-secondary" %>
<% end %>
