<%= render PageComponent.new(title: "New employee", content_container: true, full_width: false) do |component| %>
  <%= render "form", employee: @employee %>
  <%= link_to "Cancel", organization_employees_path, class: "btn btn-secondary" %>
<% end %>
