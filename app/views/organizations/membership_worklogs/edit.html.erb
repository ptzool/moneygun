<%= render PageComponent.new(title: "Editing membership worklog", content_container: true, full_width: false) do |component| %>
  <%= render "form", membership_worklog: @membership_worklog %>
  <%= link_to "Cancel", @membership_worklog, class: "btn btn-secondary" %>
<% end %>
