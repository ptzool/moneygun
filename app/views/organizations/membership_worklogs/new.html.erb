<%= render PageComponent.new(title: "New membership worklog", content_container: true, full_width: false) do |component| %>
  <%= render "form", membership_worklog: @membership_worklog %>
  <%= link_to "Cancel", membership_worklogs_path, class: "btn btn-secondary" %>
<% end %>
