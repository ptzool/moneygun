<%= render PageComponent.new(title: "Editing project", content_container: true, full_width: false) do |component| %>
  <%= render "form", project: @project %>
  <%= link_to "Cancel", @project, class: "btn btn-secondary" %>
<% end %>
