<%= render PageComponent.new(title: "New employee document type", content_container: true, full_width: false) do |component| %>
  <%= render "form", employee_document_type: @employee_document_type %>
  <%= link_to "Cancel", employee_document_types_path, class: "btn btn-secondary" %>
<% end %>
