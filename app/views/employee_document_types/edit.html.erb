<%= render PageComponent.new(title: "Editing employee document type", content_container: true, full_width: false) do |component| %>
  <%= render "form", employee_document_type: @employee_document_type %>
  <%= link_to "Cancel", @employee_document_type, class: "btn btn-secondary" %>
<% end %>
